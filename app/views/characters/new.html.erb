<div class="max-w-2xl mx-auto p-6">
  <div class="bg-white rounded-lg shadow-md p-8">
    <h1 class="text-3xl font-bold text-gray-900 mb-6">Create Your Character</h1>
    
    <p class="text-gray-600 mb-8">
      Describe your character and we'll generate their complete personality and behavior patterns using AI.
    </p>

    <%= form_with model: @character, url: characters_path, local: true, class: "space-y-6" do |form| %>
      <% if @character.errors.any? %>
        <div class="bg-red-50 border border-red-200 rounded-md p-4">
          <div class="flex">
            <div class="flex-shrink-0">
              <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
              </svg>
            </div>
            <div class="ml-3">
              <h3 class="text-sm font-medium text-red-800">
                There were some errors with your submission:
              </h3>
              <div class="mt-2 text-sm text-red-700">
                <ul class="list-disc pl-5 space-y-1">
                  <% @character.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
            </div>
          </div>
        </div>
      <% end %>

      <div>
        <%= form.label :name, class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= form.text_field :name,
                            placeholder: "e.g., Sarah, Alex, Dr. Smith",
                            class: "w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500" %>
        <p class="mt-1 text-sm text-gray-500">What should your character be called?</p>
      </div>

      <div>
        <%= form.label :description, class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= form.text_area :description,
                           rows: 6,
                           placeholder: "Describe your character's background, personality, interests, and any other details that define who they are. Be as detailed as you'd like - this will be used to generate their complete personality.",
                           class: "w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500" %>
        <p class="mt-1 text-sm text-gray-500">
          The more detail you provide, the better we can generate their personality and behavior patterns.
        </p>
      </div>

      <div>
        <%= form.label :scenario_context, "Scenario Context (Optional)", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <div class="relative">
          <%= form.text_area :scenario_context,
                             rows: 4,
                             id: "scenario_context_field",
                             placeholder: "Describe the type of scenarios, settings, or situations you plan to explore with this character. This helps ensure they're comfortable and appropriate for your intended roleplay scenarios.",
                             class: "w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500" %>
          <button type="button"
                  id="enhance_scenario_btn"
                  onclick="enhanceScenario()"
                  class="absolute bottom-2 right-2 px-3 py-1 text-xs font-medium text-white bg-purple-600 rounded hover:bg-purple-700 focus:outline-none focus:ring-2 focus:ring-purple-500 transition-colors disabled:opacity-50 disabled:cursor-not-allowed"
                  title="Use AI to enhance and expand your scenario description">
            ✨ Enhance with AI
          </button>
        </div>
        <p class="mt-1 text-sm text-gray-500">
          Examples: "Medieval fantasy adventures", "Modern detective mysteries", "Sci-fi space exploration", "Dark psychological thriller scenarios"
        </p>
        <p class="mt-1 text-sm text-purple-600">
          💡 Try the "Enhance with AI" button to expand a brief idea into a detailed scenario!
        </p>
      </div>

      <div class="bg-blue-50 border border-blue-200 rounded-md p-4">
        <div class="flex">
          <div class="flex-shrink-0">
            <svg class="h-5 w-5 text-blue-400" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd" />
            </svg>
          </div>
          <div class="ml-3">
            <p class="text-sm text-blue-700">
              <strong>How it works:</strong> After you create your character, our AI will analyze your description and generate detailed personality instructions, speech patterns, and behavioral traits. This process takes a few moments.
            </p>
          </div>
        </div>
      </div>

      <div class="flex justify-between">
        <%= link_to "Cancel", characters_path,
                    class: "px-4 py-2 text-sm font-medium text-gray-700 bg-white border border-gray-300 rounded-md shadow-sm hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
        
        <%= form.submit "Create Character",
                        class: "px-6 py-2 text-sm font-medium text-white bg-blue-600 border border-transparent rounded-md shadow-sm hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
      </div>
    <% end %>
  </div>
</div>

<script>
  async function enhanceScenario() {
    const textarea = document.getElementById('scenario_context_field');
    const button = document.getElementById('enhance_scenario_btn');
    const prompt = textarea.value.trim();
    
    if (!prompt) {
      alert('Please enter a brief scenario idea first (e.g., "a spicy rooftop romance")');
      return;
    }
    
    // Disable button and show loading state
    button.disabled = true;
    const originalText = button.innerHTML;
    button.innerHTML = '⏳ Enhancing...';
    
    try {
      const response = await fetch('<%= enhance_scenario_characters_path %>', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'X-CSRF-Token': document.querySelector('[name="csrf-token"]').content
        },
        body: JSON.stringify({ prompt: prompt })
      });
      
      if (!response.ok) {
        throw new Error('Failed to enhance scenario');
      }
      
      const data = await response.json();
      
      // Update the scenario field with the enhanced version
      textarea.value = data.scenario;
      
      // If character name was identified, show a notification
      if (data.character_name) {
        let info = `AI enhanced your scenario for character: ${data.character_name}\n\nThe character generator will use this detailed scenario to create an appropriate character.`;
        showNotification(info, 'info');
      }
      
      // Auto-resize textarea if needed
      textarea.style.height = 'auto';
      textarea.style.height = textarea.scrollHeight + 'px';
      
    } catch (error) {
      console.error('Error enhancing scenario:', error);
      alert('Failed to enhance scenario. Please try again.');
    } finally {
      // Re-enable button
      button.disabled = false;
      button.innerHTML = originalText;
    }
  }
  
  function showNotification(message, type = 'info') {
    const notification = document.createElement('div');
    notification.className = `fixed top-4 right-4 max-w-md p-4 rounded-lg shadow-lg z-50 ${
      type === 'info' ? 'bg-blue-50 border border-blue-200 text-blue-800' : 'bg-green-50 border border-green-200 text-green-800'
    }`;
    notification.style.whiteSpace = 'pre-line';
    notification.innerHTML = `
      <div class="flex items-start">
        <div class="flex-shrink-0">
          <svg class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd" />
          </svg>
        </div>
        <div class="ml-3 flex-1">
          <p class="text-sm">${message}</p>
        </div>
        <button onclick="this.parentElement.parentElement.remove()" class="ml-3 flex-shrink-0">
          <span class="text-lg">&times;</span>
        </button>
      </div>
    `;
    
    document.body.appendChild(notification);
    
    // Auto-remove after 8 seconds
    setTimeout(() => {
      notification.remove();
    }, 8000);
  }
</script>
