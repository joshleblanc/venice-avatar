json.extract! character, :id, :adult, :external_created_at, :description, :name, :share_url, :slug, :stats, :external_updated_at, :web_enabled, :created_at, :updated_at
json.url character_url(character, format: :json)
